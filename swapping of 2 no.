#include<stdio.h>
#include<conio.h>
int main()
{
	int a,b;
	printf("enter the first value a: ");
	scanf("%d", &a);
	printf(" enter the second value b: ");
	scanf("%d", &b);
	a=a+b;
	b=a-b;
	a=a-b;
	printf("now a: %d, b: %d\n",a,b);
	return 0;
}
